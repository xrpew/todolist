import React from "react";

export const TodoFooter: React.FC = () => {
  return (
    <footer className="footer-text">
      TaskFlow App • Backend en <code>Go (Gin)</code> • DB en <code>PostgreSQL</code> • Frontend en <code>Next.js</code>
    </footer>
  );
};
